import React from "react";
import classes from "./HeaderCreateAccountButton.module.css";

const HeaderCreateAccountButon = (props) => {
  return (
    <button className={classes.buttonCreateAccount}>Create Account</button>
  );
};

export default HeaderCreateAccountButon;
