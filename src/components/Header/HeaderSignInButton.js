import React from "react";
import classes from "./HeaderSignInButton.module.css";

const HeaderSignInButon = (props) => {
  return <button className={classes.buttonSignIn}>Sign In</button>;
};

export default HeaderSignInButon;
